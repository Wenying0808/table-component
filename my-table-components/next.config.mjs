/** @type {import('next').NextConfig} */
const nextConfig = {
    "output": "export",
    "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start"
    }
};

export default nextConfig;
